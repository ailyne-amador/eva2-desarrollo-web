services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: eva2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - eva2_postgres:/var/lib/postgresql/data

volumes:
  eva2_postgres:
